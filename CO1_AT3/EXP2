s = input("Enter string: ")

state = "q0"

for ch in s:
    if state == "q0":
        state = "q1" if ch == "a" else "q0"
    elif state == "q1":
        state = "q2" if ch == "b" else "q1"

if state == "q2":
    print("Accepted")
else:
    print("Rejected")
